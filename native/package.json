{
  "name": "kellerkraft-native",
  "private": true,
  "version": "1.0.0",
  "description": "Separate Capacitor iOS app. Website (repo root / GitHub Pages) is independent.",
  "scripts": {
    "build-www": "node ./scripts/build-www.mjs",
    "sync": "npm run build-www && npx cap sync",
    "sync:ios": "npm run build-www && npx cap sync ios",
    "open:ios": "npx cap open ios",
    "add:ios": "npx cap add ios",
    "import-from-web": "node ./scripts/import-from-web.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/ios": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
